Features:
  - Custom dynamic array (ArrayList-style)
    - Automatic resizing
    - .add(), .remove(), .get(), .set(), and more
    - Bounds-checked access
    
  - Unified design across structures
    - Consistent naming
    - Iterator support across lists and maps
    - Predictable behavior
    
  - Modular library structure
    - Each data structure implemented in its own class
    - Clean separation of interface and implementation
    - Easy to expand or customize
